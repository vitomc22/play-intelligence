services:
  ollama:
    image: ollama/ollama:latest
    container_name: playwright-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
      - OLLAMA_ORIGINS="*"
      - OLLAMA_CONTEXT_LENGTH=4096
      # A MÁGICA: Ativa o motor Vulkan
      #- OLLAMA_VULKAN=1

      # Precisamos devolver o acesso físico da placa gráfica para o Vulkan enxergar
    devices:
      - /dev/dri:/dev/dri

    deploy:
      resources:
        limits:
          # Liberamos 6 das 8 threads para o Ollama trabalhar pesado.
          # As 2 threads restantes garantem que o seu Linux não trave.
          cpus: '6'

    restart: unless-stopped
    network_mode: bridge
    healthcheck:
      test: [ "CMD", "ollama", "list" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  ollama_data:
    driver: local
